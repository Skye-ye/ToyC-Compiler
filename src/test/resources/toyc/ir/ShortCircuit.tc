int main() {
    int a = 5;
    int b = 0;
    int c = 10;
    
    // Test logical AND (&&) short-circuit
    int result1 = a && b;  // Should be 0 (false)
    int result5 = b || c;  // Should be 10 (true, value of c)
    return 1;
}